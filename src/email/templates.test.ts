import { describe, it } from "vitest"

describe("EmailTemplates", () => {

  it("renders confirmation email template correctly", () => {
    // arrange: input data

    // act: render template

    // assert: HTML contains expected values
  })

  it("renders release notification template correctly", () => {
    // arrange

    // act

    // assert
  })

  it("escapes unsafe user input", () => {
    // arrange: malicious input

    // act

    // assert: output is safe HTML
  })
})